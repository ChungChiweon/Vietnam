import { Boxes, PackageCheck } from 'lucide-react';
import { usePackages } from '@/context/package-context';

export default function PackageManagement() {
  const { packages, loading } = usePackages();
  return <div className="mx-auto max-w-6xl"><div className="rounded-2xl border border-black/5 bg-white p-6"><div className="flex items-center gap-3"><div className="rounded-xl bg-rose-50 p-3 text-rose-600"><Boxes className="h-5 w-5" /></div><div><p className="text-xs font-medium uppercase tracking-wider text-rose-500">B2B</p><h1 className="font-sans text-xl font-semibold">Package Management</h1></div></div><p className="mt-3 text-sm text-black/45">활성 패키지와 연결된 Product Hub 상품을 확인합니다. 생성·수정 기능은 다음 단계에서 연결합니다.</p></div>{loading ? <div className="mt-5 rounded-2xl bg-white p-10 text-center text-sm text-black/40">패키지를 불러오는 중...</div> : packages.length ? <div className="mt-5 grid gap-4 md:grid-cols-2">{packages.map((item) => <article key={item.id} className="rounded-2xl border border-black/5 bg-white p-5"><div className="flex items-start justify-between gap-3"><div><h2 className="font-semibold">{item.nameKo}</h2><p className="mt-1 text-xs text-black/40">/{item.slug}</p></div><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs text-emerald-700">Active</span></div><div className="mt-4 flex items-center gap-2 text-sm text-black/55"><PackageCheck className="h-4 w-4" />연결 상품 {item.items.length}개</div><div className="mt-3 flex flex-wrap gap-1.5">{item.targetBusinessTypes.map((type) => <span key={type} className="rounded-full bg-black/5 px-2.5 py-1 text-xs text-black/50">{type}</span>)}</div></article>)}</div> : <div className="mt-5 rounded-2xl border border-dashed border-black/10 bg-white p-10 text-center"><Boxes className="mx-auto h-8 w-8 text-black/15" /><p className="mt-3 text-sm text-black/40">등록된 활성 패키지가 없습니다.</p></div>}</div>;
}
